using System;
using UnityEngine;
using System.Collections.Generic;

public class GameManager : MonoBehaviour
{
    // Define a delegate
    // public delegate void OrderFulfilledEventHandler(Order order);

    // // Define the event using the delegate
    // public static event OrderFulfilledEventHandler OnOrderFulfilled;

    // void Update()
    // {
    //     if (Input.GetKeyDown(KeyCode.Space))
    //     {
    //         Order orderToFulfill = new Order(new List<Ingredient> { new Ingredient {name = "Tomato"}});
    //         OnOrderFulfilled?.Invoke(orderToFulfill);
    //     }
    // }
}